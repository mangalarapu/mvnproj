pipdeline{
agent any
stages{
stage('compile stage'){
steps{
withMaven(maven:"maven_HOME'){
sh'mvn clean compile'
}
}
}
stage('Testing.stage'){
steps{
withMaven(maven:'Maven_HOME'){
sh'mvn test'
}
}
}
stage('Deployment.stage'){
steps{
withMaven(maven:'Maven_HOME'){
sh'mvn Deploy'
}
}
}
}
